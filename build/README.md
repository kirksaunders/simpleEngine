Use this directory as the cmake install/binary path.

Like so:

cd build/
cmake ..

Or, use the cmake GUI and use the project root as the source code directory, and this directory as the binary directory.